<pre>
    // ==UserScript==
    // @name         AutoBalancer
    // @namespace    http://tampermonkey.net/
    // @version      2.0
    // @description  try to take over the world!
    // @author       You
    // @match        https://pools.balancer.exchange/
    // @require      https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js
    // @grant        none
    // ==/UserScript==
    
    (async function() {
        'use strict';
        jQuery.noConflict();
        const overlayContents = await (await fetch('<%=service_base_url%>/get-injectable-code')).text();
        jQuery("body").prepend(overlayContents);
    })();
</pre>