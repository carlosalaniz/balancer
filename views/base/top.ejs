<!doctype html>
<html lang="en">

<head>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css"
        integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">

    <title>Auto balancer 2.0</title>
</head>

<body>
    <%=pathname%>
    <%=locals.site.app_routing_prefix%>
    <div class="container">
        <nav class="navbar navbar-expand-lg navbar-light bg-light mb-5">
            <a class="navbar-brand" href="#">Auto balancer 2.0</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item <%=pathname === '/'?'active':''%>">
                        <a class="nav-link" href="<%=locals.site.app_routing_prefix%>/">Home</a>
                    </li>
                    <% if(locals.loggedIn) { %>
                    <li class="nav-item">
                        <a class="nav-link" href="<%=locals.site.app_routing_prefix%>/logout">Logout</a>
                    </li>
                    <%}else{%>
                    <li class="nav-item <%=pathname === '/login'?'active':''%>">
                        <a class="nav-link" href="<%=locals.site.app_routing_prefix%>/login">Log in</a>
                    </li>
                    <li class="nav-item <%=pathname === '/register'?'active':''%>">
                        <a class="nav-link" href="<%=locals.site.app_routing_prefix%>/register">Register</a>
                    </li>
                    <%}%>
                </ul>
            </div>
        </nav>